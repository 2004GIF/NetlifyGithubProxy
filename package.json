{
  "name": "github-proxy",
  "version": "1.0.0",
  "description": "GitHub proxy service for Netlify",
  "main": "netlify/functions/proxy.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "netlify build"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}