{
  "name": "nfgh-netlify-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "netlify dev",
    "preview": "netlify deploy --dir=public",
    "deploy": "netlify deploy --prod --dir=public",
    "login": "netlify login",
    "link": "netlify link"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
